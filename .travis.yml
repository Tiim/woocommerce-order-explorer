language: java

jdk:
  - oraclejdk10

before_install:
  - chmod +x gradlew

script:
  - ./gradlew assemble

deploy:
  provider: releases
  api_key: $RELEASE_TOKEN
  file: "build/lib/*.jar"
  skip_cleanup: true
  on:
    branch: master
	
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/